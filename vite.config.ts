import { defineConfig } from "vite";
// ... any other imports you already have here

export default defineConfig({
  base: '/campus-game-unite/',
  // ... leave everything else below this exactly how it was
});
